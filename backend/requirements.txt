# Core Flask application
Flask==2.3.3
Flask-CORS==4.0.0
python-dotenv==1.0.0
Werkzeug==2.3.7
gunicorn==21.2.0

# LangGraph and LangChain for agentic workflows (all use langchain-core 0.2.x)
langgraph>=0.2.0,<0.3.0
langchain>=0.2.0,<0.3.0
langchain-google-vertexai>=1.0.6,<2.0.0
# 0.2.x uses langchain-core 0.2.x (0.0.x line requires langchain-core<0.2)
langchain-community>=0.2.0,<0.3.0

# Google Cloud and Vertex AI (>=1.56.0, >=2.17.0 required by langchain-google-vertexai 1.x)
google-cloud-aiplatform>=1.56.0,<2.0.0
google-cloud-firestore==2.14.0
google-cloud-storage>=2.17.0,<3.0.0
google-cloud-secret-manager==2.17.0
# >=2.47.0 required by google-cloud-aiplatform 1.131+
google-auth>=2.47.0,<3.0.0

# Data processing and validation
pydantic==2.5.2
# pandas is a dependency of langchain but not directly used in our code
# Using newer version with better wheel support
pandas>=2.2.0,<3.0.0
numpy==1.26.2
jsonschema==4.19.2

# Caching and vector operations
redis==5.0.1
# faiss-cpu 1.7.4 not available for Python 3.14+, using latest compatible version
faiss-cpu>=1.13.0,<2.0.0
sentence-transformers==2.2.2

# RAG and document processing
PyPDF2==3.0.1
python-docx==1.1.0
beautifulsoup4==4.12.2
PyYAML==6.0.1

# API and utilities
requests==2.31.0
aiohttp==3.9.1
tenacity==8.2.3

# Testing and development
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
black==23.12.1
flake8==6.1.0
mypy==1.7.1

# Logging and monitoring
structlog==23.2.0
sentry-sdk[flask]==1.38.0

# Azure integration (for multi-cloud support)
azure-identity==1.15.0
azure-mgmt-network==25.1.0
azure-mgmt-resource==23.0.1

# Aviatrix integration (placeholder for future implementation)
# aviatrix-sdk==1.0.0  # Custom SDK to be developed