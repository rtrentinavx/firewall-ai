# Optional OpenTelemetry GCP exporters
# Install with: pip install --pre -r requirements-opentelemetry.txt
# These are pre-release packages and require the --pre flag

# GCP monitoring exporter (pre-release alpha version)
opentelemetry-exporter-gcp-monitoring>=1.11.0a0

# GCP trace exporter (stable version)
opentelemetry-exporter-gcp-trace>=1.11.0,<2.0.0
